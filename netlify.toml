[build]
  command   = "npm run tw:build && hugo --minify"
  publish   = "public"

[build.environment]
  HUGO_VERSION = "0.156.0"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/404.html"
  status = 404
